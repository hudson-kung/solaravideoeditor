"use client";

import { ChangeEvent, PointerEvent, useCallback, useEffect, useRef, useState } from "react";

type Tool="move"|"text"|"rectangle"|"circle"|"adjust";
type Layer={id:string;type:"text"|"rectangle"|"circle";name:string;visible:boolean;x:number;y:number;text?:string;fontSize?:number;color:string;width?:number;height?:number};
type Snapshot={layers:Layer[];brightness:number;contrast:number;saturation:number;zoom:number;bg:string};
const initialLayers:Layer[]=[{id:"headline",type:"text",name:"YOUR BIG IDEA",visible:true,x:70,y:310,text:"YOUR BIG IDEA",fontSize:92,color:"#ffffff"},{id:"badge",type:"rectangle",name:"Badge",visible:true,x:70,y:615,width:300,height:48,color:"#ff6719"},{id:"subtext",type:"text",name:"WATCH NOW",visible:true,x:220,y:641,text:"WATCH NOW",fontSize:32,color:"#ffffff"}];

export default function ThumbnailCreator(){
  const canvasRef=useRef<HTMLCanvasElement>(null);const inputRef=useRef<HTMLInputElement>(null);const imageRef=useRef<HTMLImageElement|null>(null);const dragRef=useRef<{x:number;y:number;layerX:number;layerY:number}|null>(null);
  const [layers,setLayers]=useState<Layer[]>(initialLayers);const [selected,setSelected]=useState("headline");const [tool,setTool]=useState<Tool>("move");const [brightness,setBrightness]=useState(85);const [contrast,setContrast]=useState(105);const [saturation,setSaturation]=useState(110);const [zoom,setZoom]=useState(100);const [bg,setBg]=useState("#1b1d20");const [imageName,setImageName]=useState("");const [history,setHistory]=useState<Snapshot[]>([]);const [future,setFuture]=useState<Snapshot[]>([]);const [saved,setSaved]=useState(true);
  const active=layers.find(l=>l.id===selected);
  const snap=useCallback(():Snapshot=>({layers:layers.map(l=>({...l})),brightness,contrast,saturation,zoom,bg}),[layers,brightness,contrast,saturation,zoom,bg]);
  const checkpoint=()=>{setHistory(h=>[...h.slice(-39),snap()]);setFuture([]);setSaved(false)};
  const draw=useCallback(()=>{const c=canvasRef.current;if(!c)return;const ctx=c.getContext("2d")!;ctx.clearRect(0,0,c.width,c.height);ctx.fillStyle=bg;ctx.fillRect(0,0,c.width,c.height);if(imageRef.current){const img=imageRef.current;ctx.save();ctx.filter=`brightness(${brightness}%) contrast(${contrast}%) saturate(${saturation}%)`;const scale=Math.max(c.width/img.width,c.height/img.height)*(zoom/100);const w=img.width*scale,h=img.height*scale;ctx.drawImage(img,(c.width-w)/2,(c.height-h)/2,w,h);ctx.restore()}for(const l of layers){if(!l.visible)continue;ctx.save();if(l.type==="text"){ctx.textAlign="center";ctx.textBaseline="middle";ctx.font=`900 ${l.fontSize}px Arial`;ctx.lineWidth=Math.max(4,(l.fontSize||30)/7);ctx.strokeStyle="rgba(0,0,0,.72)";ctx.strokeText(l.text||"",l.x,l.y);ctx.fillStyle=l.color;ctx.fillText(l.text||"",l.x,l.y)}else{ctx.fillStyle=l.color;if(l.type==="rectangle")ctx.fillRect(l.x-l.width!/2,l.y-l.height!/2,l.width!,l.height!);else{ctx.beginPath();ctx.ellipse(l.x,l.y,l.width!/2,l.height!/2,0,0,Math.PI*2);ctx.fill()}}if(l.id===selected){const w=l.type==="text"?Math.max(120,ctx.measureText(l.text||"").width):l.width!,h=l.type==="text"?(l.fontSize||30)*1.15:l.height!;ctx.setLineDash([8,6]);ctx.strokeStyle="#54a8ff";ctx.lineWidth=3;ctx.strokeRect(l.x-w/2-8,l.y-h/2-8,w+16,h+16)}ctx.restore()}},[layers,selected,bg,brightness,contrast,saturation,zoom]);
  useEffect(draw,[draw]);
  useEffect(()=>{const raw=localStorage.getItem("solara-thumbnail-design");if(!raw)return;try{const savedDesign=JSON.parse(raw) as Snapshot;setLayers(savedDesign.layers);setBrightness(savedDesign.brightness);setContrast(savedDesign.contrast);setSaturation(savedDesign.saturation);setZoom(savedDesign.zoom);setBg(savedDesign.bg);setSelected(savedDesign.layers[0]?.id||"")}catch{}},[]);
  const restore=(s:Snapshot)=>{setLayers(s.layers);setBrightness(s.brightness);setContrast(s.contrast);setSaturation(s.saturation);setZoom(s.zoom);setBg(s.bg)};
  const undo=useCallback(()=>{if(!history.length)return;const previous=history[history.length-1];setFuture(f=>[snap(),...f]);setHistory(h=>h.slice(0,-1));restore(previous)},[history,snap]);
  const redo=useCallback(()=>{if(!future.length)return;setHistory(h=>[...h,snap()]);restore(future[0]);setFuture(f=>f.slice(1))},[future,snap]);
  useEffect(()=>{const key=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==="z"){e.preventDefault();e.shiftKey?redo():undo()}if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==="y"){e.preventDefault();redo()}if((e.key==="Delete"||e.key==="Backspace")&&!(e.target instanceof HTMLInputElement)&&!(e.target instanceof HTMLTextAreaElement))deleteLayer()};window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key)});
  const patchLayer=(patch:Partial<Layer>,remember=false)=>{if(remember)checkpoint();setLayers(ls=>ls.map(l=>l.id===selected?{...l,...patch}:l));setSaved(false)};
  const addLayer=(type:Layer["type"])=>{checkpoint();const id=crypto.randomUUID();const layer:Layer=type==="text"?{id,type,name:"New text",visible:true,x:640,y:360,text:"NEW TEXT",fontSize:72,color:"#ffffff"}:{id,type,name:type==="rectangle"?"Rectangle":"Circle",visible:true,x:640,y:360,width:260,height:150,color:"#ff6719"};setLayers(l=>[...l,layer]);setSelected(id);setTool("move")};
  const deleteLayer=()=>{if(!selected)return;checkpoint();setLayers(ls=>ls.filter(l=>l.id!==selected));setSelected(layers.find(l=>l.id!==selected)?.id||"")};
  const duplicate=()=>{if(!active)return;checkpoint();const copy={...active,id:crypto.randomUUID(),name:`${active.name} copy`,x:active.x+25,y:active.y+25};setLayers(l=>[...l,copy]);setSelected(copy.id)};
  const moveLayer=(dir:number)=>{const index=layers.findIndex(l=>l.id===selected),next=index+dir;if(index<0||next<0||next>=layers.length)return;checkpoint();const copy=[...layers];[copy[index],copy[next]]=[copy[next],copy[index]];setLayers(copy)};
  const upload=(e:ChangeEvent<HTMLInputElement>)=>{const file=e.target.files?.[0];if(!file)return;checkpoint();setImageName(file.name);const img=new Image();img.onload=()=>{imageRef.current=img;draw();setSaved(false)};img.src=URL.createObjectURL(file)};
  const point=(e:PointerEvent<HTMLCanvasElement>)=>{const r=e.currentTarget.getBoundingClientRect();return{x:(e.clientX-r.left)*1280/r.width,y:(e.clientY-r.top)*720/r.height}};
  const down=(e:PointerEvent<HTMLCanvasElement>)=>{if(tool!=="move"||!active)return;checkpoint();const p=point(e);dragRef.current={x:p.x,y:p.y,layerX:active.x,layerY:active.y};e.currentTarget.setPointerCapture(e.pointerId)};
  const move=(e:PointerEvent<HTMLCanvasElement>)=>{if(!dragRef.current)return;const p=point(e),d=dragRef.current;patchLayer({x:Math.max(0,Math.min(1280,d.layerX+p.x-d.x)),y:Math.max(0,Math.min(720,d.layerY+p.y-d.y))})};
  const up=()=>{dragRef.current=null};
  const newDocument=()=>{checkpoint();setLayers(initialLayers);setSelected("headline");setBg("#1b1d20");setBrightness(85);setContrast(105);setSaturation(110);setZoom(100);setImageName("");imageRef.current=null;setSaved(false)};
  const exportPng=()=>{setSelected("");setTimeout(()=>{draw();const a=document.createElement("a");a.download="solara-thumbnail.png";a.href=canvasRef.current!.toDataURL("image/png");a.click();setSelected("headline")},0)};
  const saveDesign=()=>{localStorage.setItem("solara-thumbnail-design",JSON.stringify(snap()));setSaved(true)};
  return <main className="thumb-app"><header className="thumb-top"><a href="/dashboard"><span>✦</span>SOLARA</a><div><b>Thumbnail Creator</b><small>Untitled design · {saved?"Saved locally":"Unsaved changes"}</small></div><section><button className="thumb-save" onClick={saveDesign}>Save</button><button onClick={exportPng}>Export PNG ↗</button></section></header><div className="thumb-menu"><button onClick={newDocument}>File · New</button><button onClick={()=>inputRef.current?.click()}>Open image</button><button onClick={undo} disabled={!history.length}>Undo</button><button onClick={redo} disabled={!future.length}>Redo</button><button onClick={duplicate}>Duplicate</button><button onClick={deleteLayer}>Delete</button><span/><small>Ctrl+Z Undo · Delete removes layer</small></div><section className="thumb-workspace"><aside className="thumb-tools"><button className={tool==="move"?"active":""} onClick={()=>setTool("move")}>↖<small>Move</small></button><button onClick={()=>inputRef.current?.click()}>▧<small>Image</small></button><button className={tool==="text"?"active":""} onClick={()=>{setTool("text");addLayer("text")}}>T<small>Text</small></button><button onClick={()=>addLayer("rectangle")}>▱<small>Rectangle</small></button><button onClick={()=>addLayer("circle")}>●<small>Circle</small></button><button className={tool==="adjust"?"active":""} onClick={()=>setTool("adjust")}>◐<small>Adjust</small></button></aside><aside className="thumb-assets"><h2>{active?.type==="text"?"Text properties":active?"Shape properties":"Design"}</h2><button className="upload-image" onClick={()=>inputRef.current?.click()}>＋ Upload image</button><input ref={inputRef} type="file" accept="image/*" hidden onChange={upload}/>{active&&<><p className="thumb-label">SELECTED LAYER</p><label>Name<input value={active.name} onChange={e=>patchLayer({name:e.target.value})}/></label>{active.type==="text"&&<><label>Text<textarea value={active.text} onChange={e=>patchLayer({text:e.target.value,name:e.target.value})} rows={3}/></label><label className="range-label">Font size <b>{active.fontSize}px</b><input type="range" min="16" max="180" value={active.fontSize} onChange={e=>patchLayer({fontSize:+e.target.value})}/></label></>}<label>Color<input type="color" value={active.color} onChange={e=>patchLayer({color:e.target.value})}/></label>{active.type!=="text"&&<div className="format-row"><label>Width<input type="number" value={active.width} onChange={e=>patchLayer({width:+e.target.value})}/></label><label>Height<input type="number" value={active.height} onChange={e=>patchLayer({height:+e.target.value})}/></label></div>}<div className="layer-actions"><button onClick={duplicate}>Duplicate</button><button onClick={()=>moveLayer(1)}>Move up</button><button onClick={()=>moveLayer(-1)}>Move down</button><button className="danger" onClick={deleteLayer}>Delete</button></div></>}<p className="thumb-label">IMAGE ADJUSTMENTS</p><label className="range-label">Brightness <b>{brightness}%</b><input type="range" min="20" max="160" value={brightness} onChange={e=>{setBrightness(+e.target.value);setSaved(false)}}/></label><label className="range-label">Contrast <b>{contrast}%</b><input type="range" min="20" max="180" value={contrast} onChange={e=>{setContrast(+e.target.value);setSaved(false)}}/></label><label className="range-label">Saturation <b>{saturation}%</b><input type="range" min="0" max="200" value={saturation} onChange={e=>{setSaturation(+e.target.value);setSaved(false)}}/></label><label className="range-label">Image zoom <b>{zoom}%</b><input type="range" min="80" max="200" value={zoom} onChange={e=>{setZoom(+e.target.value);setSaved(false)}}/></label><label>Canvas color<input type="color" value={bg} onChange={e=>{setBg(e.target.value);setSaved(false)}}/></label></aside><div className="thumb-stage"><div className="canvas-wrap"><canvas ref={canvasRef} width="1280" height="720" onPointerDown={down} onPointerMove={move} onPointerUp={up}/></div><div className="zoom-status">50% · 1280 × 720 · {imageName||"No background image"}</div></div><aside className="layers-panel"><div className="layers-title"><h3>Layers</h3><button onClick={()=>addLayer("text")}>＋</button></div>{[...layers].reverse().map(l=><div key={l.id} className={`layer ${selected===l.id?"active":""}`} onClick={()=>{setSelected(l.id);setTool("move")}}><span>{l.type==="text"?"T":l.type==="circle"?"●":"▱"}</span><div><b>{l.name}</b><small>{l.type}</small></div><button onClick={e=>{e.stopPropagation();patchLayer.call(null,{visible:!l.visible});setLayers(ls=>ls.map(x=>x.id===l.id?{...x,visible:!x.visible}:x))}}>{l.visible?"◉":"○"}</button></div>)}<div className="layer-buttons"><button onClick={()=>addLayer("text")}>＋ Text</button><button onClick={()=>addLayer("rectangle")}>＋ Shape</button></div></aside></section></main>;
}
