export type StoredProject = { id:string; name:string; fileName:string; video:Blob; duration:number; editState:Record<string,unknown>; thumbnail?:string; updatedAt:string };
const DB_NAME="solara-projects"; const STORE="projects";
function db(){return new Promise<IDBDatabase>((resolve,reject)=>{const request=indexedDB.open(DB_NAME,1);request.onupgradeneeded=()=>{if(!request.result.objectStoreNames.contains(STORE))request.result.createObjectStore(STORE,{keyPath:"id"})};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}
export async function putProject(project:StoredProject){const database=await db();return new Promise<void>((resolve,reject)=>{const tx=database.transaction(STORE,"readwrite");tx.objectStore(STORE).put(project);tx.oncomplete=()=>{database.close();resolve()};tx.onerror=()=>reject(tx.error)})}
export async function getProject(id:string){const database=await db();return new Promise<StoredProject|undefined>((resolve,reject)=>{const request=database.transaction(STORE).objectStore(STORE).get(id);request.onsuccess=()=>{database.close();resolve(request.result)};request.onerror=()=>reject(request.error)})}
export async function listProjects(){const database=await db();return new Promise<StoredProject[]>((resolve,reject)=>{const request=database.transaction(STORE).objectStore(STORE).getAll();request.onsuccess=()=>{database.close();resolve((request.result as StoredProject[]).sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt)))};request.onerror=()=>reject(request.error)})}
export async function removeProject(id:string){const database=await db();return new Promise<void>((resolve,reject)=>{const tx=database.transaction(STORE,"readwrite");tx.objectStore(STORE).delete(id);tx.oncomplete=()=>{database.close();resolve()};tx.onerror=()=>reject(tx.error)})}
